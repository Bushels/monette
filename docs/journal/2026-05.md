# Journal — May 2026

## 2026-05-01 — MPS Portfolio Cleanup

**Performed by:** MPS portfolio-cleanup workflow (Codex gpt-5.5)

### Changes applied
- .gitignore: unblocked .claude/agents/** and .claude/skills/** (was globally ignored under .claude/). Added narrow !docs/journal/** exception inside the ignored docs/ block.
- .claude/agents/.gitkeep: created to track empty agents directory.
- .claude/skills/.gitkeep: created to track skills directory root.
- .claude/skills/farmland-legal-descriptions/SKILL.md: was on disk but untracked — now staged for tracking. Retained as local skill (Monette-specific DLS/PLSS domain knowledge; not promoted to global).
- .claude/launch.json: remains gitignored (absolute Windows path, non-portable IDE config).
- AGENTS.md: created (rules-only, no activity log).
- CLAUDE.md: created (rules-only, no activity log).
- GEMINI.md: not created — no evidence Gemini is in use on this repo.
- .codex/config.toml: created with gpt-5.5 / reasoning_effort = "medium".
- PROJECT_STATE.md: created with last verified commit + filled-in active task / blockers / next action (sourced from docs/logs/seeding-calibration.md).
- .gitignore: added narrow !docs/logs/** exception for tracked methodology logs.
- docs/logs/seeding-calibration.md: moved from repo root (was SEEDING_CALIBRATION_LOG.md) — now tracked. Reframed from working-note convention to first-class methodology doc per user.
- CLAUDE.md: added rule pointing future sessions to PROJECT_STATE.md at session start.

### Post-cleanup follow-up
- Moved 12 mid-iteration GEE seeding-calibration files (UI + pipeline + tests + build) from main's working tree to a new branch `feat/seeding-calibration` (commit `2632275`). Main is now clean and shippable. PROJECT_STATE.md updated to point at the branch.

### Skipped / out of scope
- No model-ref bumps needed: no claude-opus-4-0/1/5/6 references found in any tracked file.
- No global skill promotions: farmland-legal-descriptions is Monette-specific only.
- No GEMINI.md deletion needed: file was never present.

## 2026-05-01 — SK Titles Update plan drafted (awaiting sign-off)

**Performed by:** Claude (Opus 4.7), session-driven plan draft

User delivered 3 ISC-style title export CSVs (`docs/Land/Monette - file 1.csv`, `file 2.csv`, `file 3.csv`) representing all land titled to MONETTE FARMS LTD. as of 2026-01-18. Inventory: 559 unique surface parcels across 20 SK Rural Municipalities, plus 10 incidental mineral titles.

### Documents created
- `docs/superpowers/specs/2026-05-01-sk-titles-update-plan.md` (local-only) — full plan with 5 phases, branch strategy, Codex checkpoints, and 4 open questions
- `docs/logs/sk-titles-2026-01-18.md` (tracked) — methodology log, anchor-truth principle, skills/agents tracker, per-area completion ledger (empty pending sign-off)

### Key findings
- Top-5 RMs by parcel count: AUVERGNE (85), ENFIELD (77), WHISKA CREEK (65), LAJORD (65), MORSE (64)
- RM MORSE 165 has 64 parcels but no current "morse" property record — likely folds into `calderbank` (Enfield)
- RM DOUGLAS (Hafford area) shows only 2 of the supposedly 158 Hafford quarters — corroborates the rented-back / Walter Farms thesis (Hafford bulk was never titled to MFL)
- RM LAJORD T15-R18-W2M (65 parcels) + RM EDENWOLD T16-R18-W2M (14 parcels) are contiguous and likely both anchor Regina-South

### Branch strategy
- New branch `feat/sk-titles-2026-01-18` to be created after sign-off (does NOT touch existing `feat/seeding-calibration` GEE branch)
- Area-by-area verification gates; no Phase 2 area moves forward without Codex per-area review + browser smoke test
- Final merge to main only after Codex round 3 + Vercel preview verified

### Status
- Awaiting user sign-off on:
  1. Branch name
  2. Wave ordering (easy → combined → uncertain → new → town-lots)
  3. "Existing data not in CSV stays as-is, flag for follow-up" rule
  4. Codex 5.5 budget (2–3 rounds total + per-area spot-checks vs full per-area reviews)

## 2026-05-01 — SK Titles Update SHIPPED to main + origin

**Performed by:** Claude (Opus 4.7), full session (Phases 0-5 + atlas marker-hide + post-merge fix)

**Final shipped state on main:** commit `58634f9`, pushed to origin Bushels/monette.

### What landed across the session
- 17 commits on `feat/sk-titles-2026-01-18` (SK Titles Update from 2026-01-18 ISC snapshot)
- 1 commit on `feat/atlas-hide-property-markers` (UX polish — Montana sub-property dot suppression)
- 2 `--no-ff` merge commits on main (`fdd806c`, `194dd32`)
- 2 follow-up commits on main (`8a6b42a` state update, `58634f9` Codex post-merge audit follow-up)

### Net effect
- 559/559 ISC titles reconciled, 1,418 records carry `mflTitleSnapshot` metadata
- swift-current 0 → 28 mapped MFL-titled parcels (transitioned from `geometryStatus: point-only`)
- regina-south 0 → 120 mapped MFL-titled parcels (same transition)
- vanguard 93 → 105 (Neville town lots folded in, 3 runtime-key collisions resolved across 2 sessions)
- wymark 101 → 85 (16 Stewart Valley parcels reassigned to swift-current)
- hafford 158 → 158 records, but now visibly flagged as 2 of 158 MFL-titled — Walter Farms / rented-back thesis surfaced in property record's `mflTitleSnapshot.significance`
- 5 Montana sub-properties got `hideMapMarker:true` (UX polish)
- 159 polygons computed via DLS quarter math + new LSD math + Plan N3619 placeholders

### Codex 5.5/xhigh involvement (4 passes)
1. Round-1 worked example on Wymark + Swift Current (`5380074`) — methodology playbook produced
2. Round-2 final review on the 13-commit branch — returned NEEDS-FIXES (2 BLOCKERS + 2 SHOULD-FIXES); fixes in `64d2e91`
3. Post-merge audit on shipped main — returned SHIPPED-WITH-FOLLOW-UPS (1 vanguard runtime-key collision); fix in `58634f9`
4. Implicit fix verification: each fix re-confirmed by re-running the same checks Codex used

### Methodology playbook captured
- New memory file: `~/.claude/projects/G--My-Drive-Agriculture-Monette/memory/sk_titles_methodology.md`
- THE trap: title rows ≠ map features. Dedup at runtime identity (`property_id:loc`), not at semantic identity (`(loc, rm)`)
- FLAG taxonomy with 6 subtypes, RM-name-based defaults
- LSD geometry rules (fractal of section-grid math at 1/4 resolution)
- Knowledge layer separated from geometry/title layer

### 8 new tracked scripts
- `parse_isc_titles.py` (CSV → normalized JSON with crosswalk)
- `audit_isc_vs_quarters.py` (diff + runtime-key invariant check)
- `build_deltas_from_audit.py` (Codex-pattern delta generator)
- `apply_sk_titles_deltas.py` (replaces property arrays from deltas)
- `compute_sk_titles_polygons.py` (DLS quarter + LSD math)
- `_dedup_quarters_by_loc_rm.py` (now loc-only dedup, mirrors runtime identity)
- `_fold_neville_into_vanguard.py` (Phase 3 one-off)
- `_update_data_js_totals.py` (surgical data.js updater)

## 2026-05-02 — Atlas dot cleanup (farmland-wide hideMapMarker)

**Performed by:** Claude (Opus 4.7), session-driven UX polish

User feedback on the live site: "the dots on the map should only represent things that are not farmland or farm areas for example, the Pea Plant or Seed facility. It is becoming too cluttered with dots and it is taking away from the maps. Notes for the area will go into left side hovers or hover features."

### Change
Extended the existing `hideMapMarker:true` flag (introduced 2026-05-01 for Montana sub-properties) to ALL farmland/farm-area property records. Only 3 facilities keep their dots:
- `outlook-seeds` — Outlook Seeds Plant
- `lethbridge-pea-protein` — Lethbridge Pea Protein Facility
- `tonopah` — Tonopah Seeds Facility

### Properties newly flagged (21 total)
Script-flagged (19): admiral, calderbank, hafford, kamsack, outlook, ponteix, prince-albert, raymore, vanguard, wymark, eddystone, the-pas, airdrie, swift-current, regina-south, bc-ranches, goats-peak, aguila, genoa.

Manually flagged (2): cabri-bank, rosetown — these record-formats put `lat/lng` on the line below the id-line, and the script's regex required them on the same line.

### Tooling
- New `scripts/_apply_farmland_hide_marker.py` — one-off migration with allow-list (3 facilities) + scope-aware regex (only operates inside the `properties: [` array, not `soldProperties:`)

### Sold-property markers preserved
The 3 standalone sold markers (`sold-hafford-phase-2`, `sold-havre`, `sold-regina-i`) keep their red dots — they're on a separate `SOLD_ASSET_LAYER`, not the property-pin layer. The single existing `sold-wymark-waldeck` already had `hideMapMarker:true` from a prior commit (its 5 sold quarters render as red sold-parcels on the Wymark cluster instead).

### Mobile-first awareness
Removing dots reduces visual clutter at small breakpoints — fewer label collisions on mobile pan/zoom, smoother scroll. The left-rail property cards (per user's screenshot of the rendered list view) already adapt via existing responsive media queries.

### Cache-bust
data.js v=38 → v=39.
