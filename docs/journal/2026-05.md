# Journal — May 2026

## 2026-05-01 — MPS Portfolio Cleanup

**Performed by:** MPS portfolio-cleanup workflow (Codex gpt-5.5)

### Changes applied
- .gitignore: unblocked .claude/agents/** and .claude/skills/** (was globally ignored under .claude/). Added narrow !docs/journal/** exception inside the ignored docs/ block.
- .claude/agents/.gitkeep: created to track empty agents directory.
- .claude/skills/.gitkeep: created to track skills directory root.
- .claude/skills/farmland-legal-descriptions/SKILL.md: was on disk but untracked — now staged for tracking. Retained as local skill (Monette-specific DLS/PLSS domain knowledge; not promoted to global).
- .claude/launch.json: remains gitignored (absolute Windows path, non-portable IDE config).
- AGENTS.md: created (rules-only, no activity log).
- CLAUDE.md: created (rules-only, no activity log).
- GEMINI.md: not created — no evidence Gemini is in use on this repo.
- .codex/config.toml: created with gpt-5.5 / reasoning_effort = "medium".
- PROJECT_STATE.md: created with last verified commit + filled-in active task / blockers / next action (sourced from docs/logs/seeding-calibration.md).
- .gitignore: added narrow !docs/logs/** exception for tracked methodology logs.
- docs/logs/seeding-calibration.md: moved from repo root (was SEEDING_CALIBRATION_LOG.md) — now tracked. Reframed from working-note convention to first-class methodology doc per user.
- CLAUDE.md: added rule pointing future sessions to PROJECT_STATE.md at session start.

### Post-cleanup follow-up
- Moved 12 mid-iteration GEE seeding-calibration files (UI + pipeline + tests + build) from main's working tree to a new branch `feat/seeding-calibration` (commit `2632275`). Main is now clean and shippable. PROJECT_STATE.md updated to point at the branch.

### Skipped / out of scope
- No model-ref bumps needed: no claude-opus-4-0/1/5/6 references found in any tracked file.
- No global skill promotions: farmland-legal-descriptions is Monette-specific only.
- No GEMINI.md deletion needed: file was never present.

## 2026-05-01 — SK Titles Update plan drafted (awaiting sign-off)

**Performed by:** Claude (Opus 4.7), session-driven plan draft

User delivered 3 ISC-style title export CSVs (`docs/Land/Monette - file 1.csv`, `file 2.csv`, `file 3.csv`) representing all land titled to MONETTE FARMS LTD. as of 2026-01-18. Inventory: 559 unique surface parcels across 20 SK Rural Municipalities, plus 10 incidental mineral titles.

### Documents created
- `docs/superpowers/specs/2026-05-01-sk-titles-update-plan.md` (local-only) — full plan with 5 phases, branch strategy, Codex checkpoints, and 4 open questions
- `docs/logs/sk-titles-2026-01-18.md` (tracked) — methodology log, anchor-truth principle, skills/agents tracker, per-area completion ledger (empty pending sign-off)

### Key findings
- Top-5 RMs by parcel count: AUVERGNE (85), ENFIELD (77), WHISKA CREEK (65), LAJORD (65), MORSE (64)
- RM MORSE 165 has 64 parcels but no current "morse" property record — likely folds into `calderbank` (Enfield)
- RM DOUGLAS (Hafford area) shows only 2 of the supposedly 158 Hafford quarters — corroborates the rented-back / Walter Farms thesis (Hafford bulk was never titled to MFL)
- RM LAJORD T15-R18-W2M (65 parcels) + RM EDENWOLD T16-R18-W2M (14 parcels) are contiguous and likely both anchor Regina-South

### Branch strategy
- New branch `feat/sk-titles-2026-01-18` to be created after sign-off (does NOT touch existing `feat/seeding-calibration` GEE branch)
- Area-by-area verification gates; no Phase 2 area moves forward without Codex per-area review + browser smoke test
- Final merge to main only after Codex round 3 + Vercel preview verified

### Status
- Awaiting user sign-off on:
  1. Branch name
  2. Wave ordering (easy → combined → uncertain → new → town-lots)
  3. "Existing data not in CSV stays as-is, flag for follow-up" rule
  4. Codex 5.5 budget (2–3 rounds total + per-area spot-checks vs full per-area reviews)
